Replace TEAMID in .well-known/apple-app-site-association with the Apple Team ID.
Deploy with firebase deploy --only functions:pulsetrackr-sos,hosting --project pulsetracker-0000.
Set the chosen domain in app/ShareConfig.swift.
Set the chosen domain in DeepLinkRouter.allowedHosts.
AASA changes need an app reinstall to re-fetch.
